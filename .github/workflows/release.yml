name: Release

# Manually dispatched stable release. The version is either given explicitly
# or auto-bumped from the latest stable tag (patch by default); the workflow
# builds first and creates the git tag at publish time — you never push tags
# by hand. A version with a pre-release suffix (1.2.3-rc.1) is honored as a
# manual beta escape hatch: it ships to the beta channel and the GitHub
# Release is marked pre-release (never "Latest"). The normal beta path is the
# nightly build (see nightly.yml).
on:
  workflow_dispatch:
    inputs:
      version:
        description: Version to release (e.g. 1.2.3). Leave empty to auto-bump from the latest stable tag.
        required: false
        type: string
        default: ""
      bump:
        description: Segment to bump when version is empty.
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
        default: patch

permissions:
  contents: write

# Serialize against nightly.yml (same group): both publish a tag, a GitHub
# Release, and the shared appcast on gh-pages. queue: max makes queued runs
# wait their turn — the default would silently CANCEL a pending release when
# the nightly cron fires while another publish is still running.
concurrency:
  group: publish
  cancel-in-progress: false
  queue: max

jobs:
  version:
    name: Compute version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.compute.outputs.version }}
      tag: ${{ steps.compute.outputs.tag }}
      channel: ${{ steps.compute.outputs.channel }}
      prerelease: ${{ steps.compute.outputs.prerelease }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          fetch-tags: true
      - name: Compute version
        id: compute
        env:
          EXPECTED_VERSION: ${{ inputs.version }}
          VERSION_BUMP: ${{ inputs.bump }}
        run: bash scripts/ci/compute_version.sh release

  publish:
    name: Publish ${{ needs.version.outputs.tag }}
    needs: version
    uses: ./.github/workflows/build-publish.yml
    with:
      version: ${{ needs.version.outputs.version }}
      tag: ${{ needs.version.outputs.tag }}
      channel: ${{ needs.version.outputs.channel }}
      prerelease: ${{ needs.version.outputs.prerelease == 'true' }}
    secrets: inherit
